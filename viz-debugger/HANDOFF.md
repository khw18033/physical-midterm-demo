# 노드 캔버스를 봐주세요

`마일스톤 목록 → 노드 캔버스 (실행 노드 + 팔레트에서 꺼낸 뷰 노드) → 확대`

> **2026-09-03 — 탭이 없어졌습니다.** 탭②~⑤의 화면 넷은 캔버스에 놓는 **뷰 노드**의
> 확대로 들어갔습니다. 요구는 하나도 죽지 않았습니다(45 → 50건, 삭제 0건). 탭 다섯이
> 살아 있는 마지막 지점은 태그 `5tab-baseline` 이고 그것이 **논문 §3-2 요인 B의 대조군**
> 입니다 — 「탭 조건 vs 노드 조건」 비교가 그 태그 하나로 성립합니다.
>
> 이식본 넷은 기존 프로토타입에서 **동작을 그대로 옮겨 온 것**이라 배치·색·여백이 캔버스와
> 다릅니다. **이번 HCI 논의 대상은 캔버스 화면입니다** — 확대 안쪽의 화면 정리는 다음 차례입니다.

> 탭⑥(파이프라인 편집기)은 그보다 앞선 2026-08-31에 제거했습니다 — 노드 에디터 방향이
> 이 캔버스로 확정됐기 때문입니다.

# HCI 전달본 — 임무 디버깅 가시화 초안

`npm run build`의 `dist/`는 통합 셸(캔버스 + 뷰 노드 넷)이며, `npm run build:standalone`의 `dist-standalone/`는 캔버스만 담는다(뷰 노드 렌더러를 주입하지 않아 팔레트가 뜨지 않는다). 둘 다 임무 `MSN-260826-01` 데이터를 번들에 내장하므로 게이트웨이 없이도 화면이 채워진다. 로컬 개발은 `npm run dev`로 실행한다.

## 화면 다섯 개

1. **마일스톤 진입 화면**: 발화 패널, 마일스톤 7건, 하드웨어 7대를 함께 본다. 하드웨어 카드를 마일스톤에 드롭하면 하위 태스크의 대상 표시가 바뀌고, 마일스톤 클릭으로 그래프에 진입한다.
   **발화 패널은 이제 목이 아닙니다** — 마이크로 말하면 실제로 인식되고, 파형은 실제 입력 레벨이며, 인식된 문장을 그 자리에서 고칠 수 있습니다(원문과 수정본을 둘 다 보관합니다). 진행 막대 다섯 칸 중 **앞 두 칸만 실제**이고, 뒤 세 칸(`의도 분석`·`마일스톤 분리`·`태스크 생성`)은 아직 목이라 `목` 배지가 붙어 있습니다. STT 서비스를 띄우지 않으면 음성 기능만 꺼지고 문장을 직접 넣는 경로가 열립니다.
   *(2026-08-31)* 문장이 **시나리오 대본에 매칭되면** 뒤 세 칸이 `대본` 배지(초록)로 바뀝니다 — `의도 분석 → 대본 조회` · `마일스톤 분리 → 대본에서 읽음` · `태스크 생성 → 대본에서 읽음`. 맞은 키워드와 고른 대본 ID가 그 자리에 뜨고, 이것은 LLM이 아니라 **키워드 대조**입니다. 이 화면(전달본)의 스크린샷 기준은 여전히 **`MSN-260826-01` 기동 상태**입니다 — 대본은 승인해야만 화면을 바꿉니다.
2. **태스크 그래프**: MS-C의 분기·합류 DAG와 태스크 상태, 배정 장비 연결 상태를 본다. 스크래치 블록처럼 노드를 드래그해 옮길 수 있고 연결선이 따라간다. 노드를 더블클릭하면 액션 상세로 내려간다. *(2026-09-04)* **DAG/트리 토글은 없어졌다** — 회의록 §10의 「그래프 형태 vs 트리 형태」가 **그래프로** 닫혔기 때문이다(요구사항정의서 §7.10). 대신 머리줄이 그 임무 자신의 모양을 적는다(`태스크 DAG — 7노드 · 합류 1`). 트리와의 비교는 화면이 아니라 측정 도구(`npm run measure:representation`)와 논문에서 다룬다. *(2026-08-31)* 그 옆에 **`[이 마일스톤] [임무 전체]` 범위 토글**이 붙었다 — 분기·합류가 마일스톤을 건너기 때문이고, 기본은 마일스톤이라 이 전달본의 화면 흐름은 그대로다. 되돌아가는 참조가 마일스톤 밖으로 나가면 그래프 아래에 `↺` 한 줄과 「임무 전체로 보기」가 뜬다. *(2026-09-01)* 머리줄 맨 위에 **이동 경로**(`마일스톤 › MS-… › T-…`)가 붙었고 맨 앞 칸을 누르면 마일스톤 목록으로 돌아간다 — 그 전에는 되돌아가는 길이 상단 바의 임무 이름 버튼뿐이었다. 그래프는 **자리 폭을 재서 밴드로 접는다**(가로로 늘어나지 않는다). **전달본 임무 `MSN-260826-01` 자체는 무수정**이라 이 화면의 노드 구성은 바뀌지 않았다(노드 분화는 대본 3편에만 적용).
3. **D-03 액션 아이템 상세**: T-31의 네 액션과 Unitree Go1 상태 스트립, 평가 근거를 한 팝업에서 본다. 버튼 조작은 메모리와 콘솔에 `produced_by=human`으로 남는다.
4. **D-04 리플레이**: D-02와 같은 그래프에 재생 시각을 주입한다. 슬라이더를 움직이면 해당 시점 상태로 다시 칠하고, 아래 타임라인과 임무 이력을 함께 본다.
5. **D-05 실패 수정**: T-35 실패와 역방향 관련 노드를 강조한다. 팝업 입력값을 바꿔 반사실 실행 또는 재실행을 눌러볼 수 있다.

초안은 정보 구조 논의를 위한 것이며 색, 여백, 애니메이션, 반응형은 의도적으로 다듬지 않았다. 모든 자료는 목 데이터다.

## 논의하고 싶은 항목 8건

1. **계층 전환** — 세 계층을 한 화면에 둘 것인가, 계층별로 전환할 것인가
2. **상태 여덟 개의 표시** — 색·테두리·아이콘·회차 배지를 어떻게 조합할 것인가. 특히 건너뜀과 미수행이 한눈에 구분되어야 한다
3. **실패 경로 격리의 표현** — 나머지를 숨길 것인가, 흐리게 할 것인가, 접을 것인가
4. **되감기 조작** — 슬라이더·단계 이동·사건 지점 점프 중 무엇을 줄 것인가
5. **사용자 계층별 표시 깊이** — 비전문가에게 무엇을 감출 것인가
6. **하드웨어 배정 방식** — 드래그로 마일스톤에 놓는 방식이 자연스러운가
7. **명령 버튼의 배치** — 보는 화면에 실행 버튼을 섞을 때의 오조작 위험
8. **인식 결과 수정을 어디서 하게 할 것인가** — 발화 패널 안인가, 별도 확인 단계인가. 지금은 패널 안에서 바로 고치게 되어 있는데, 잘못 인식된 문장이 이미 화면에 보이는 상태에서 고치는 것이 편한지 아니면 "이 문장이 맞습니까" 단계를 따로 두는 것이 안전한지가 갈립니다. 신뢰도가 낮을 때는 지금도 재확인 체크를 요구합니다
