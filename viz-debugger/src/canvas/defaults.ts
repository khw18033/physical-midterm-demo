/**
 * src/canvas/defaults.ts (260903 — 1단계)
 *
 * **층 ① — 대본이 주는 기본 구성** (`VZ-N-04`). 읽기 전용이고, 사용자 구성이 **없을 때만**
 * 이긴다. 되돌리기(층 ③)가 사용자 구성을 지우면 다음 적재가 여기로 온다.
 *
 * ## 1단계에서는 어느 대본도 기본 구성을 주지 않는다 — 자리만 만든다
 *
 * 기본 구성은 **대본 사이드카**(읽기 전용)에 둔다 — `scenarios/<id>.match.json` 과 같은
 * 방식이다. 대본 JSON 본문에 좌표를 적지 않는 이유는 F10 `REQ-1002` 와 같다: 이야기와
 * 화면 배치는 수명이 다르고, 배치를 고치자고 대본을 고치면 `verify:scenario`(옛 편은 한
 * 글자도 고치지 않는다)와 부딪힌다.
 *
 * 사이드카 파일 자체는 **2단계**에 만든다. 4종 요약 카드가 아직 자리표시라 「기본으로 어떤
 * 노드를 어디에 놓아 둘까」를 정할 근거가 없고, 지금 정하면 2단계에 다시 고치게 된다.
 * 그래서 1단계는 **읽는 경로만** 뚫어 두고 항상 null 을 돌려준다 — 그 결과 읽는 순서는
 * `② → 빈 캔버스` 로 동작하며, ①이 생겨도 호출부는 한 줄도 바뀌지 않는다.
 */

import type { CanvasConfig } from './persist.ts';

/** 사이드카가 들어오면 두 인자를 다 쓴다 — 그때 이름의 밑줄만 뗀다. */
export function defaultCanvasConfig(_missionId: string, _slot: string): CanvasConfig | null {
  return null;
}
