# -*- coding: utf-8 -*-
# ↑ pip 은 requirements.txt 를 로케일 인코딩(한국어 Windows: cp949)으로 읽는다.
#   한글 주석이 있으면 이 줄이 없을 때 UnicodeDecodeError 로 설치가 통째로 실패한다.
# stt-lab/requirements.txt 에서 실험용을 뺀 것. 그 이상을 넣지 않는다.
#
# PyTorch를 끌어오지 않는다. faster-whisper는 CTranslate2 런타임이라 torch가 필요 없고,
# torch가 의존성에 들어왔다면 엔진을 잘못 잡은 것이다 (`pip list` 에 torch 가 보이면 확인할 것).
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
faster-whisper>=1.1.0
