# 탐지 ↔ 로봇(pi7) — 받아야 하는 데이터 (2026-09-14)

로봇 담당에게 전달하는 문서다. 관제 웹 쪽 규약은
[`탐지_연동스키마_260912.md`](탐지_연동스키마_260912.md) 에 있고, **이건 그 반대편** —
탐지가 로봇에서 받아야 하는 것만 적었다.

브로커(`ws://pi7.tailcb6bfb.ts.net:9001/mqtt`)에는 2026-09-14 11:20경 붙어서 25초 동안
들어 봤다. 아래 「이미 있는 것」은 그때 실제로 받은 값이다.

## 요점 하나

**프레임 한 장에 그 프레임을 찍은 각도가 같이 붙어 있어야 한다.**

이것 하나가 없어서 지금 파이프라인이 못 돈다. 나머지는 대부분 이미 브로커에 있다.

---

## 0. 지금 브로커에 이미 있는 것 (확인함)

| 토픽 | 주기 | 우리가 쓰나 |
|---|---|---|
| `zoneA/robot/go1-001/state` | 5초 (`state_interval_s: 5.0`) | **쓴다** — `position.heading_deg` |
| `zoneA/robot/go1-001/status` | 이벤트 | 참고만 — `media`, `robot_mode`, `in_mission` |
| `zoneA/robot/go1-001/heartbeat` | 5초 | 안 쓴다 |
| `zoneA/sensor/wl-001/*` | — | 안 쓴다 (수위 센서) |
| `zoneA/robot/go1-sim/*`, `terminal/go1-sim/sim/state` | — | 안 쓴다 (시뮬레이터) |

**카메라 프레임은 없었다.** `status` 가 이렇게 알려 준다.

```json
"media": { "streaming": false, "source": "test",
           "size": "1280x720", "fps": 15, "codec": "mjpeg/rtp" }
```

영상이 MJPEG/RTP 별도 스트림이고 지금은 꺼져 있다(`streaming: false`, `source: "test"`).
25초 동안 이미지 페이로드는 한 건도 없었다.

---

## 1. 반드시 받아야 하는 것

### ① 스캔 프레임 8장 — **각도를 함께** (제일 중요)

우리 처리는 프레임마다 `(이미지, 회전각)` 이 **짝으로** 필요하다. 각도는 나중에
「문이 어느 방향에 있나」를 계산하는 기준이 되므로, 짝이 어긋나면 결과가 통째로 틀어진다
(45도 어긋나면 로봇이 45도 엉뚱한 데로 돈다).

**`state` 의 `heading_deg` 를 시각으로 짝지어 쓰는 것은 안 된다.** 5초 주기 주기보고라
45도 회전이 그보다 빠르면 어느 프레임이 몇 도인지 가를 수 없다. 각도는 **프레임을 찍은
그 순간의 값**이어야 한다.

전송은 아래 셋 중 하나면 된다. **A안을 권한다** — 각도가 프레임과 같은 메시지에 실려
오므로 짝을 맞출 일이 아예 없고, 우리 수신기가 이미 이 모양을 읽는다(코드 수정 불필요).

| 안 | 방법 | 평가 |
|---|---|---|
| **A (권장)** | MQTT 한 메시지에 JPEG + 각도 | 짝 문제 없음. 바로 붙는다 |
| B | RTP 스트림 + MQTT 캡처 이벤트 | 기존 mjpeg/rtp 구조 활용. RTP 주소 필요, 시각 동기 필요 |
| C | 우리 쪽 HTTP 로 업로드 | 우리가 엔드포인트를 열면 된다. 로봇이 POST 를 보내야 함 |

**A안 페이로드** — 토픽은 기존 작명 그대로 `zoneA/robot/go1-001/frame` 를 제안한다.

```json
{
  "schema_version": "1.3",
  "device_id": "go1-001",
  "channel": "frame",
  "timestamp": "2026-09-16T10:00:03+0900",
  "seq": 0,
  "rotation_deg": 0.0,
  "image": "<JPEG 를 base64 로>"
}
```

- `rotation_deg` — **0, 45, 90, 135, 180, 225, 270, 315** (오른쪽=시계 방향). 이름은
  `rotation` / `angle` / `yaw` / `heading` 중 아무거나여도 우리가 읽는다.
- `seq` — 0~7. 없으면 도착 순서로 센다.
- `image` — base64. 원시 JPEG 바이트를 그대로 실어도 읽는다(그때 각도는 토픽 끝에
  `.../frame/45` 처럼 붙이면 된다).
- 크기는 1280×720 JPEG 한 장이 대략 100~300KB, base64면 1.33배다. 8장이면 3MB 남짓이라
  한 번에 보내도 된다. **브로커에 메시지 크기 제한(`message_size_limit`)이 걸려 있으면
  알려 달라** — 그 경우 B안으로 간다.

### ② 회전 방향과 시작 기준

- **오른쪽(시계) 방향으로 45도씩 8번**이 전제다. 반대로 돌면 좌우가 뒤집힌다.
- `rotation_deg = 0` 인 첫 프레임이 **스캔 시작 시점의 로봇 정면**이다. 그 절대 방위는
  우리가 단상 관측으로 역산하므로, 특정 방향을 향할 필요는 없다.
- 스캔 시작 위치는 **단상이 어느 한 프레임에라도 보이는 자리**여야 한다. 단상이 8장
  전부에서 안 보이면 자기 위치 추정 자체가 실패한다(그때는 정직하게 실패로 보고한다).

### ③ 촬영 조건

- **정지한 뒤에 찍어야 한다.** 회전 중 촬영은 흔들려서 탐지·depth 둘 다 망가진다.
  45도 돌고 → 멈추고 → 찍고 → 다음, 순서면 된다.
- 로봇이 빨리 돌아도 상관없다. 우리 쪽 처리가 프레임당 약 2초라 큐에 쌓아 두고 순서대로
  처리한다. 다만 관제 웹 화면이 「한 각도씩 결과가 느는」 모양을 기대하므로, 한 번에
  8장을 몰아 보내는 것보다 **찍는 대로 한 장씩** 보내 주는 편이 낫다.
- **이미지를 가공하지 말 것** — 자르기·리사이즈·회전·보정 전부. 우리 쪽에서 해상도에
  맞춰 처리한다. 가공된 프레임이 오면 박스 좌표와 거리 계산이 전부 어긋난다.

### ④ 스캔 경계 신호

한 판이 시작·종료됐다는 것만 알면 된다. 별도 토픽이 없어도 `status` 의
`in_mission` 이 `false → true → false` 로 바뀌면 그걸로 쓰겠다. **다른 방식이면 알려 달라.**

---

## 2. 시연 전에 한 번만 받으면 되는 것

### ⑤ 실제 카메라로 찍은 샘플 프레임 8장 ← **가장 급하다**

지금 탐지 파이프라인의 문 판정 기준(색·모양·채도·기준영상 유사도)은 **이전 카메라의
464×400 프레임**(테두리를 잘라낸 237×241 영역)에 맞춰 실측으로 튜닝한 값이다. GO1
카메라는 1280×720이라 밝기·색감·화각이 다르면 이 기준이 그대로는 안 맞을 수 있다.

**시연 환경에서 실제로 한 바퀴 돌며 찍은 8장만 미리 주면** 우리가 기준을 다시 맞춰
둔다. 이게 없으면 시연 당일에 처음 보는 프레임으로 맞춰야 한다.


---

## 3. 안 줘도 되는 것

혹시 준비 중이면 미리 접어도 된다.

- **depth / 거리 측정값** — 우리가 UniDepth 로 직접 낸다
- **물체 탐지 결과** — 그게 우리 일이다
- 배터리, 수위 센서, heartbeat, 시뮬레이터 토픽
- 관제 웹으로 가는 것 — 탐지 결과는 우리가 직접 HTTP 로 연다(연동스키마 §1)

---

## 4. 우리가 돌려줄 것 — 받을 토픽을 알려 달라

8프레임 스캔이 끝나면 이동 지시를 산출한다. **로봇이 이걸 받아 움직여야 하면** 어느
토픽으로 받을지 알려 달라(예: `zoneA/robot/go1-001/cmd`). 지금은 보내지 않고 있다.

```json
{
  "target_class": "door",
  "turn_instruction": "왼쪽(반시계)으로 90.0도 회전",
  "turn_deg": -90.0,
  "forward_distance_cm": 614.3,
  "standoff_cm": 80.0
}
```

- 「문 앞 **80cm** 에 정지」가 목적지다.
- 회전 → 직진, 두 동작이면 끝난다.
- 장애물 회피는 우리가 하지 않는다(단상과 겹치는지만 검사해서 알려 준다).

---

## 5. 정리 — 지금 막혀 있는 것

| # | 필요한 것 | 상태 |
|---|---|---|
| ① | 프레임 8장 + **각도** | ❌ MQTT 에 없음. `media.streaming: false` |
| ② | 회전 방향·시작 기준 확인 | ❓ 확인 필요 |
| ③ | 정지 후 촬영 / 가공 없는 원본 | ❓ 확인 필요 |
| ④ | 스캔 경계 | △ `in_mission` 으로 대체 가능 |
| ⑤ | 실제 카메라 샘플 8장 | ❌ **가장 급함** |
| — | MQTT 연결 자체 | ✅ 구독 확인됨 |
| — | 테일스케일 | ✅ 양쪽 도달 확인 (pi7 7ms) |
