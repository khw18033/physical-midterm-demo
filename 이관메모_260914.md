# 이관 메모 (2026-09-14) — 새 기기에서 이어서 할 것

시연 2026-09-16. 지금까지의 작업 PC(`ubuntu3-15ug50p-gp55kn`)에 **NVIDIA GPU가 없어서**
파이프라인 실물 구동을 못 했다. GPU가 있는 기기로 옮겨 이어 간다.

---

## 1. 왜 기기를 옮기나

| 항목 | 확인값 |
|---|---|
| GPU | ❌ Intel Iris Xe 내장만 (`lspci`), NVIDIA 커널 모듈 미적재 |
| `nvidia-smi` | 실패 (드라이버 없음) |
| 파이프라인 요구 | `device="cuda"` **하드코딩** — CLIP/YOLO/GroundingDINO/FastSAM/UniDepth 전부 |

그래서 **탐지·depth 본체는 이 세션에서 한 번도 실행되지 않았다.** 지금까지 검증한 것은
입출력 계층(MQTT 수신기, HTTP 창구)뿐이고, 방법은 stub 서비스와 합성 픽스처였다.

새 기기에서 **가장 먼저 할 일**: `mock_stream_receiver.py`로 데이터셋 8장을 돌려
파이프라인이 끝까지 가는지 확인. 그게 되어야 로봇 연동을 얹는 의미가 있다.

---

## 2. Tailscale 연결 정보

테일넷 **`tailcb6bfb.ts.net`** (계정 `khw671@`). 로봇·관제 웹·탐지 전부 이 위에 있다.

| 노드 | Tailscale IP | 역할 |
|---|---|---|
| `pi7` | 100.72.109.9 | 로봇 라즈베리파이 (MQTT 브로커 + 카메라 브리지) |
| `laptop-isk6l1rq` | 100.78.11.92 | 관제 웹(가시화) |
| `ubuntu3-15ug50p-gp55kn` | 100.86.206.127 | 탐지 (← **이 자리를 새 기기가 대신한다**) |
| `desktop-oaujese` | 100.125.71.51 | (참고) |

### 새 기기에서 붙기

```bash
curl -fsSL https://tailscale.com/install.sh | sh
sudo tailscale up --accept-dns=true
#   → 출력된 https://login.tailscale.com/a/... 를 브라우저로 열어
#     위 테일넷을 소유한 계정(khw671@)으로 로그인

tailscale status | grep pi7          # 로봇이 보이는지
tailscale ping pi7                   # 도달하는지 (실측 7ms)
```

**이미 다른 테일넷에 붙어 있다면** `sudo tailscale logout` 후 다시 `up`. 한 대가 두
테일넷에 동시에 붙을 수는 없다.

### 방화벽

이전 PC는 ufw가 `ENABLED=no`라 막는 게 없었다. 새 기기에서 ufw가 켜져 있으면 관제 웹이
우리 창구에 못 닿는다:

```bash
sudo ufw status verbose
sudo ufw allow in on tailscale0      # 테일넷에서 오는 건 허용
```

### ⚠ 관제 웹에 주소를 다시 알려 줘야 한다

관제 웹이 폴링할 주소는 **기기의 MagicDNS 이름**이라 기기가 바뀌면 주소도 바뀐다.

```
http://<새-기기-hostname>.tailcb6bfb.ts.net:8000
```

`tailscale status`의 첫 줄이 자기 이름이다. 관제 웹의 「연결 관리 → 객체 탐지」에
이 주소를 넣어야 한다. 안 바꾸면 화면에 「닿지 않습니다」만 뜬다.

관제 웹이 HTTPS로 서비스되면 브라우저가 http 요청을 mixed content로 막는다. 그때는
`tailscale serve --bg 8000`으로 https를 씌운다.

### 로봇 MQTT

```
ws://pi7.tailcb6bfb.ts.net:9001/mqtt     (생 TCP 1883도 열려 있음)
```

브로커에 메시지 크기 제한은 없다(`message_size_limit` 미설정, 로봇 팀 확인).

---

## 3. 환경 설치 — 겪은 함정 포함

```bash
git clone https://github.com/JNY03/physical_demo.git && cd physical_demo
python3 -m venv venv
source venv/bin/activate
pip install -r requirements-freeze.txt
pip install -e "git+https://github.com/lpiccinelli-eth/UniDepth.git@8d8cfe4c7ee15297099983607febf0d4f32eb3d6#egg=unidepth"
pip install "paho-mqtt>=2.0"          # MQTT 수신기 전용(freeze 에 없음)
```

**함정 1 — `python3.14 -m venv` 에 pip 이 안 들어갈 수 있다.** 이전 PC가 그랬다
(`ensurepip` 모듈 자체가 없음). 그때는 이렇게 우회한다:

```bash
python3.14 -m venv venv
curl -sS https://bootstrap.pypa.io/get-pip.py | venv/bin/python
venv/bin/python -m pip -V
```

(배포판에 `python3.14-venv` / `python3-pip` 패키지가 있으면 그걸 설치하는 게 낫다.)

**함정 2 — `tokenizer.json` 이 저장소에 없었다.** `BatchClipEngine`이 이 파일을 읽어서
없으면 첫 명령에서 바로 죽는다. **이번 커밋에 포함시켰으므로 이제 clone 만으로 해결된다.**
혹시 또 없으면:

```bash
curl -sSL -o models/openvocab/clip-vit-base-patch32/tokenizer.json \
  "https://huggingface.co/openai/clip-vit-base-patch32/resolve/main/tokenizer.json?download=true"
```

**함정 3 — 최초 실행 시 HuggingFace 다운로드**가 있다(`grounding-dino-tiny`,
`unidepth-v2-vitb14`). 인터넷이 되는 상태에서 한 번 돌려 캐시를 채워 두고 시연에 들어갈 것.

---

## 4. 실행

```bash
cd demo/test

# (a) 먼저 이것부터 — 데이터셋 8장 재생으로 파이프라인 전체 확인
python3 mock_stream_receiver.py

# (b) 로봇 연동
python3 mqtt_stream_receiver.py --target door
python3 mqtt_stream_receiver.py --sniff        # 브로커에 뭐가 흐르는지만 보기

# (c) 관제 웹 창구 (별도 터미널, torch 없이도 뜬다)
python3 detect_api_server.py --port 8000
```

**둘 다 미리 띄워 둬야 한다.** 관제 웹이 MQTT로 명령을 보내도 프로세스가 켜지지 않는다
(관제 웹은 HTTP GET만 던진다). 수신기는 시작할 때 모델 4개를 올리느라 수십 초 걸리므로
발표 직전에 띄우면 첫 스캔을 놓친다.

---

## 5. 두 문서 반영 내역 (스키마 관련 수정)

규약 문서 두 개가 authoritative 하다. 코드가 그 모양을 따라간다.

| 문서 | 방향 | 반영된 곳 |
|---|---|---|
| `탐지_연동스키마_260912.md` | 관제 웹 → 탐지 | `detect_api_server.py` |
| `detection-protocol_0914.md` | 로봇 → 탐지 | `mqtt_stream_receiver.py` |
| `탐지_로봇데이터요구_260914.md` | 탐지 → 로봇 (우리가 보낸 요구) | — |

### 5-1. 관제 웹 창구 (`detect_api_server.py`, 신규)

`try1/`을 **읽기만** 하는 얇은 어댑터. 표준 라이브러리만 써서 ML 환경 없이도 뜬다.
창구 9개: `/health`, `/detect/localization`, `/results`, `/evidence`, `/path`,
`/features`, `/frame`, `/path_overlay`, `/map`.

- 응답 모양은 연동스키마 §1 예시 그대로. 내부 JSON과 이름이 다른 두 곳은 어댑터가 흡수:
  - `final_score` → 화면이 「특징 최고값」으로 쓰므로 **CLIP 특징 유사도의 최댓값**을 내보낸다
    (내부 `final_score`는 지지 provider 신뢰도의 **합**이라 의미가 다르다 → `fusion_confidence_sum`)
  - `mandatory_gates` → 내부 `clip_detail.gates`를 `color`→`color_gate` 식으로 개명
- CORS `Access-Control-Allow-Origin: *` 를 **그림 응답 포함 전부**에 붙인다
- 아직 없는 것은 **이유를 본문에 담은 404** (화면은 「아직 안 왔다」로 처리하고 멈추지 않는다)
- 매 요청마다 디스크를 다시 훑어서 각도가 끝날 때마다 배열이 하나씩 는다 — **캐시 금지**

### 5-2. 로봇 수신 (`mqtt_stream_receiver.py`, 신규)

- 구독: `zoneA/robot/go1-001/frame`(방향마다 1건) + `/scan`(판의 시작·끝), QoS 1
- **판(scan round) 단위**로 돈다. `scan_start`/`scan_end`/`mission_id`가 경계. 새 판이
  시작되면 이전 판 산출물·탐지 누적·depth 캐시를 초기화한다(안 지우면 버린 판 결과가
  관제 웹 화면에 남는다). scan 이벤트가 유실돼도 프레임 수를 채우면 끝낸다.
- **중복 그림이 섞인 판은 통째로 버린다.** Go1 카메라는 얼어붙어도 계속 내보내고 그림만
  멈춘다. 로봇의 `duplicate_of_prev`에 더해 판 안의 sha1을 우리도 모아 **건너뛴 중복까지**
  잡는다(로봇은 직전 한 장하고만 견준다). 같은 사진 8장을 8방향으로 오해하면 문 방향이
  통째로 틀어지므로 **이 검사를 완화하면 안 된다.**
- `yaw_deg`는 판마다 기준점이 옮겨져 방향 계산에 쓸 수 없다 → `ROTATION_KEYS`에서 일부러
  뺐다. 각도는 언제나 `rotation_deg`(0/45/…/315, 오른쪽=시계).
- 관제 웹이 명령을 보내지 않으므로, 판이 정상 종료되면 **자동으로** 위치추정 → 관측요약
  → 경로 산출까지 끝낸다.

### 5-3. 해상도 — 464×400 이다

요구 문서에 「GO1 카메라 1280×720」이라 적었던 것은 **틀렸다.** `status.media`의
`"size": "1280x720"`은 아직 안 쓰는 별도 송출기 값이고(`"source": "test"`), 실제 정면
카메라는 **464×400** 이다(로봇 팀 확인 + 프레임마다 `width`/`height` 동봉).

데이터셋과 화소가 같아서 **테두리 자르기(237×241)와 거리 보정식(`image_w=464`)이 그대로
적용된다.** 기존 CLIP 게이트 튜닝값을 손댈 필요가 없다.

### 5-4. 로봇 명령 어휘 (`navigate_to_target_service.py`)

`try1/navigation/evidence.json`에 로봇이 바로 쓸 숫자값을 넣었다.

```json
"turn_deg": -90.0,
"forward_distance_m": 6.354,
"robot_command": {
  "turn": { "deg": -90.0 },
  "move_forward": { "distance_m": 6.354 },
  "distance_m_in_range": true,
  "note": "turn.deg는 오른쪽이 +(왼쪽이 음수). move_forward.distance_m에는 standoff가 이미 반영돼 있다 -- 또 빼지 말 것."
}
```

- 부호 규약이 로봇과 일치한다(**오른쪽이 +**)
- `standoff_cm`(문 앞 80cm)은 이미 `forward_distance` 에서 빠져 있다 — 로봇이 또 빼면 안 된다
- 허용 범위 0.05~10m를 벗어나면 `distance_m_in_range: false` + `warning`
- **로봇 명령 경로는 protobuf**(`terminal/go1-001/downlink`)라 우리는 **발행하지 않는다.**
  `--publish-topic`을 줄 때만 JSON을 쏜다. 번역 토픽이 열리면 위 값을 그대로 넘기면 된다.

### 5-5. 프레임 원본 출처 (실시간 대응)

`navigate`가 depth를 다시 잴 때 예전에는 데이터셋 폴더에서만 프레임을 찾아서 로봇
실시간 프레임을 열 수 없었다. `FRAME_SOURCE_DIR`로 **명시**하게 바꿨다(수신기가
`try1/incoming`으로 설정). 자동 추측을 넣으면 안 된다 — 로봇 프레임 이름이
`frame_000001.jpg`처럼 데이터셋과 겹쳐서 엉뚱한 이미지의 depth를 재게 된다.
테두리 자르기 오프셋도 해상도 조건부로 바꿨고, UniDepth에 경로 단위 캐시를 붙였다.

---

## 6. 남은 일

| # | 할 일 | 상태 |
|---|---|---|
| 1 | **데이터셋 8장으로 파이프라인 실물 검증** | ❌ GPU 없어서 못 함 — 새 기기에서 최우선 |
| 2 | 단상이 안 보여도 동작하는 대체 위치추정 | ◐ 바탕만 깔림(아래) |
| 3 | systemd 서비스 등록(부팅·크래시 자동 복구) | ❌ 미착수 |
| 4 | 관제 웹에 새 기기 주소 재공지 | ❌ 기기 옮긴 뒤 |
| 5 | 로봇 실물 한 바퀴 돌려 프레임 수신 확인 | ❌ |

### 2번 진행 상황과 설계 방향

`_bearing_refinement()`를 독립 메서드로 **분리해 뒀다.** 이 계산은 타겟 자신의 관측만
쓰므로 **로봇의 지도상 위치를 몰라도 성립한다** — 즉 단상이 안 보여도 **회전각은 구할 수
있다.** (분리 전과 동일하게 동작하는 것은 검증했다.)

아직 안 한 것은 그 아래다. 설계 방향은 이렇다.

- **위치는 단상 없이 구할 수 없다** — 미지수 `x, y, heading` 3개에 랜드마크 하나는 구속이
  2개뿐이라 미결정이다. 문 자신의 관측각으로 오프셋을 구하면 순환 논리가 된다.
- **거리도 문 depth로는 못 구한다** — 문은 멀어서 `rel_depth`가 보정식 유효범위(≤4.0)를
  벗어난다(실측 10.02).
- 그래서 단계적으로 낮춰 가는 설계가 맞다:
  - **A (현행)** 단상 관측 있음 → 위치·방위·거리 전부
  - **B (구현 예정)** 단상 없음, 문 관측 있음 → **회전각만** 산출. 위치·거리는 `null` +
    이유. 로봇은 일단 문을 보도록 돌 수 있다.
  - **C (선택)** 문의 겉보기 크기로 거리 근사. `거리 × 박스폭(px) = 상수`를 A가 성공한
    판에서 자동으로 역산해 저장해 두고, 단상이 없을 때 꺼내 쓴다. 데이터셋 실물 구동이
    되어야 상수를 얻을 수 있으므로 **1번 다음**이다.
  - **D** 아무 랜드마크도 없음 → 정직하게 실패 보고(현행 유지)

경로 산출에 정말 필요한 것은 회전각과 문까지의 거리이지 로봇의 지도상 위치가 아니다.
지도 위 로봇 점은 그림용이다 — B/C만 되어도 로봇은 움직일 수 있다.
