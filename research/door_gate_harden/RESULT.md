# 문(door) 게이트 강화 — 2026-09-15

사용자 지시: "`try1/incoming` 환경에서 파란 문은 frame_000006 쪽에만 있는데 단상과 사람의 다리를 가끔 문으로 오검출.
게이트 강화 필요. 실험 후 수정 계획 먼저 보고" → 보고 후 "1단계만 실제 판에 반영하고 재시작해 기능 검증".

**운영 반영**: `demo/test/class_features.json`(door: `hue_gate`, `confirm_pair_iou_min`) +
`class_finder_service.py`(`_median_hue`, `_dictionary_ovd_pair_iou`, `_resolve_lifecycle` 확정 조건) +
`detect_api_server.py`(`hue` → `hue_gate`, 추가 필드 `dictionary_ovd_iou`). 원본은 `backup_260915/`.
실험 스크립트·캐시는 운영 폴더 밖(세션 scratchpad)에서 돌렸고, 여기에는 반영 후 검증(`verify_e2e.py`)만 둔다.

## 데이터
- `datasets/260915` 기록 23건 중 로봇 프레임이 있는 14건(106장). 카메라가 언 161113 은 서로 다른 앞 3장만, 170008·170351 제외.
  - 로봇 프레임 = 수신 원본(해시 일치). 탐지 코드·설정은 09-14 18:34 이후 그대로 → 기록의 판정 = 운영값.
  - 운영값 오프라인 재현이 기록 판정과 106/106장, 박스까지 일치.
- 정답(육안): 실제 문 21개(방 270°·315° 19, 복도 90°·135° 2). **방 판 12개 중 11개에서 315°(frame_000007)에도 같은 파란 문이 보여** 정답으로 셌다.
- 기록 속 실제 오탐 6건: 164814 45°·164923 45°·165313 90°·165734 90°(단상), 165313 180°(청바지 다리), 170907 135°(화이트보드·사람).
- 보강: 기록 13건 × 변형 6종(dim/gamma_light/cool/shift_l/noise/squash), 17:10 판 × 변형 15종, 09-14 회귀 rot8·robot × 11조건. 합계 문 209개.

## 원인
오탐 6건 전부 **작은 CLIP 통과 조각(창문·짙은 옷)이 GroundingDINO 의 큰 단상/사람 'door' 박스 안에 containment 0.7 로만 묶여**
"서로 다른 provider 2개 동의"가 성립했다.

| | CLIP 병합 박스 ↔ GDINO 박스 IoU | 포함률 | GDINO door 점수 |
|---|---|---|---|
| 실제 오탐 6건 | 0.03 ~ 0.22 | 0.75 ~ 1.0 | 0.27 ~ 0.37 |
| 실제 문 | 0.76 ~ 0.91 | 0.96 ~ 1.0 | 0.52 ~ 0.75 |

변형 포함 분포: 실제 문 최소 0.471(85건) / 오탐 최대 0.378(12건).

## 후보 비교 (문 209개)
| 설정 | 문 | 오탐 |
|---|---|---|
| 기존 운영값 | 195 | 62 |
| 짝 IoU ≥ 0.35 | 195 | 4 |
| 짝 IoU ≥ 0.40 / 0.42 | 195 | 3 |
| 짝 IoU ≥ 0.45 | 194 | 3 |
| 짝 IoU ≥ 0.50 | 188 | 2 |
| containment 연계 끔(door) | 195 | 13 |
| GDINO door ≥ 0.35 | 188 | 24 |
| 색 tol 0.006 | 194 | 51 |
| 파란 화소 비율 ≥ 0.30 | 183 | 17 |
| 색상(H) 중앙값 90~112 단독 | 195 | 17 |
| **반영: 짝 IoU ≥ 0.42 + 색상 H 중앙값 85~112** | **195** | **0** |

- 짝 IoU 로 남는 3건은 모두 합성 변형(cool/shift_l)의 사람 다리 — CLIP·GDINO 가 같은 다리를 함께 잡음. 색상 게이트가 막는다.
- 색상 상한 108~114 에서 오탐 0·문 손실 0 (106 은 클러스터가 바뀌어 새 오탐 1, 118 은 다리 오탐 재발). 문 기여 후보 H p5~p95 100~105, 최대 111 / 다리 105~125.
- 기록 14건의 "문이 보인 방향": 기존 5건 틀림(평균 방위 32~45° 틀어짐, 165734 는 문 없는 90°) → 반영 후 14건 모두 정답.

## 업스케일링(참고, 반영 안 함)
기록 14 + rot8 + robot 원본(문 25) 기준, 기존 운영값 게이트:

| 방식 | 문/오탐 | AUC sim | AUC 색차 | 추가 시간/장 |
|---|---|---|---|---|
| 원본 | 25/25, 6 | 0.933 | 0.965 | - |
| bicubic x2 | 25/25, 8 | 0.934 | 0.962 | 사전 +140ms |
| CLIP 패딩(가운데 자르기 대신) | 25/25, 7 | 0.842 | 0.940 | - |
| Swin2SR x2 (CLIP crop 만) | 25/25, 8 | 0.950 | 0.970 | ~530ms |
| Swin2SR x2 (전체) | 25/25, 4 | 0.958 | 0.950 | ~510ms |
| Swin2SR x4 실사진 (전체) | 24/25, 23 | 0.935 | 0.951 | ~540ms |
| Swin2SR x4 압축영상 (전체) | 23/25, 18 | 0.920 | 0.976 | ~520ms |

강화 게이트와 함께: robot 방 11조건 12/22 → Swin2SR x2 crop 16/22(오탐 1: 문 옆 유리장), 기록 원본+변형 131/135 → 133/135(오탐 0).
업스케일은 오탐 원인(증거 연계)을 풀지 못하고, bicubic 은 모델이 내부에서 어차피 늘려 효과 없음, x4 는 OVD 헛박스가 늘어 해롭다.
Swin2SR x2 crop 은 어두운 조건 검출률이 조금 오르지만 판당 +4초·새 모델 의존·점수 분포 이동이 따라 별도 결정으로 남긴다.

## 반영 후 검증(운영 코드, 패치 없음) — `verify_e2e.py`
출력은 `e2e/<세트>/`(try1 무관), 요약 `e2e/summary.json`.
- 16세트(기록 14 + rot8 + robot) 전부 통과: 문 25/25, 오탐 0, 문이 보인 회전각 = 정답.
- 기록의 실제 문 23개 박스는 기존 운영 판정과 IoU 1.0(그대로). rot8 0.897/0.81, robot 0.786/0.969(육안 정답 대비, 09-14 와 동일 수준).
- 확정된 문의 짝 IoU 최소 0.506. evidence.json 에 `gates.hue`(median_hue) 와 `evidence_trail[].dictionary_ovd_iou` 가 남는다.
- pedestal: rot8 에서 09-14 운영 e2e(`door_gate_relax/e2e/prod`)와 박스까지 동일(39·57 프레임).

## 한계
- 실제 오탐 표본은 기록 6건, 나머지는 합성 변형. 정답·ambiguous 는 육안.
- 색상 게이트는 이 파란 문 전용(색·채도 게이트와 같은 성격) — 다른 색 문은 원래도 못 잡는다.
- pedestal 에는 새 규칙을 걸지 않았다.
- `dictionary_ovd_iou` 는 "보이는 상태"가 바뀐 시점의 기록 값이다(그 뒤 증거로 값만 바뀌면 새 기록이 생기지 않는다). 확정 판정 자체는 매 증거마다 다시 계산한다.
